
<div class="container">

    <%= simple_form_for(@ape) do |f| %>
        <%= f.input :name %>
        <%= f.input :address,
                input_html: { data: { geocoder_target: "address" } },
                wrapper_html: { data: { controller: "geocoder", geocoder_api_key_value: ENV["MAPBOX_API_KEY"] } } %>
        <%= f.submit "Submit review", class: "secondary-button" %>
    <% end %>

</div>



